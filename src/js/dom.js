// Export para btn-sandwish.js
const sandwishButton = $(".btnSandwish");
const iconHash = $(".iconHash");
const dropdownMenu = $(".menuSuspenso");
const closeMenu = $(".closeMenu");
const pathElement = $(".btnSandwishSvgPath");
const spanBlack = $(".spanBlack");
const pathActived = "M3.75 9h16.5m-16.5 6.75h16.5";
const pathDisabled = "M6 18L18 6M6 6l12 12";
//
// Export para card.js
const radioButtons = $('input[type="radio"]');
const checkButton = $(".check-button");
const cardSearch = $(".cardSearch");
//
// Export para main-details.js
const body = $("#body");
//
// Export para btn-clear.js
const svgSearch = $(".svgSearch");
const pokeSearchInput = $(".pokeSearch");
const clearButton = $(".clearButton");
const main = $(".main");

export {
    sandwishButton,
    iconHash,
    dropdownMenu,
    closeMenu,
    pathElement,
    spanBlack,
    pathActived,
    pathDisabled,
    radioButtons,
    checkButton,
    cardSearch,
    body,
    svgSearch,
    pokeSearchInput,
    clearButton,
    main,
  };
